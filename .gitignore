venv/
__pycache__/
*.pyc

# Raw downloaded BTS zip/CSV dumps (multi-GB) - re-downloaded on demand by
# airport_stats_tool.py on cache miss, not meant to be committed.
data_cache/

# Transient test-run logs from development, not part of the deliverable.
*.log

.idea/

# Local working draft - final DESIGN.md is written by hand from this, not committed as-is.
DESIGN_DRAFT.md

# Downloaded Piper voice models (~180MB) - re-downloadable via
# `python3 -m piper.download_voices`, same pattern as data_cache/.
piper_voices/
